version: 0.2

# VoteCapsule — Candidate Service Rebuild
# Triggered when candidate entity/DTO changes (e.g. is_youth, is_plwd columns)

env:
  variables:
    AWS_REGION: "us-east-1"
    ECR_REGISTRY: "683541453923.dkr.ecr.us-east-1.amazonaws.com"
    NODE_SLIM: "683541453923.dkr.ecr.us-east-1.amazonaws.com/ecr-public/docker/library/node:22-slim"

phases:
  install:
    commands:
      - echo "Candidate service rebuild"
      - docker --version

  pre_build:
    commands:
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY
      - export SHORT_SHA=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c1-8)
      - echo "SHA = $SHORT_SHA"
      - docker pull $NODE_SLIM || echo "WARN node:22-slim pull failed"

  build:
    commands:
      - echo "--- Building candidate-service ---"
      - docker pull $ECR_REGISTRY/vote-capsule/candidate-service:latest 2>/dev/null || true
      - docker build
          --cache-from $ECR_REGISTRY/vote-capsule/candidate-service:latest
          --build-arg SERVICE_PORT=3005
          --build-arg GIT_SHA=$CODEBUILD_RESOLVED_SOURCE_VERSION
          -t $ECR_REGISTRY/vote-capsule/candidate-service:$SHORT_SHA
          -t $ECR_REGISTRY/vote-capsule/candidate-service:latest
          -f services/candidate/Dockerfile .
      - docker push $ECR_REGISTRY/vote-capsule/candidate-service:$SHORT_SHA
      - docker push $ECR_REGISTRY/vote-capsule/candidate-service:latest
      - echo "DONE candidate-service"

  post_build:
    commands:
      - echo "=== Rolling vc-candidate ==="
      - aws ecs update-service --cluster vote-capsule-services --service vc-candidate --force-new-deployment --region $AWS_REGION || true
      - echo "=== Done ==="
